import { ConditionalTruncatePipe } from './conditional-truncate.pipe';

describe('ConditionalTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionalTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
